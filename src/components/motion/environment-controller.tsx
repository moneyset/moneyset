"use client";

/**
 * @deprecated Superseded by `MarketMotionProvider` (atmosphere + motion intelligence).
 * Kept for compatibility — no-op render.
 */
export function EnvironmentController() {
  return null;
}
